type Art @entity {
    id: ID!
    price: BigInt
    remainingAmount: BigInt
    Token: [Token!]! @derivedFrom(field: "art")
}

type Token @entity {
    id: ID!
    art: Art
    owner: String!
    tokenURI: String
}
